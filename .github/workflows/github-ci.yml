name: Test Pipeline

on:
  push:
    branches:
      - main

jobs:
  docker-job:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Docker Compose Up
        run: |
          docker-compose up-d --name nginx -p 80:80 nginx
